<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.chart.PieChart?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="controller.MainController">

    <!-- Top -->
    <top>
        <ToolBar>
            <Label text="Personal Finance Manager" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>
        </ToolBar>
    </top>

    <!-- Center: Table -->
    <center>
        <TableView fx:id="tableView">
            <columns>
                <TableColumn fx:id="colDate" text="Date" prefWidth="100"/>
                <TableColumn fx:id="colCategory" text="Category" prefWidth="150"/>
                <TableColumn fx:id="colAmount" text="Amount" prefWidth="100"/>
                <TableColumn fx:id="colDescription" text="Description" prefWidth="250"/>
            </columns>
        </TableView>
    </center>

    <!-- Right: Chart -->
    <right>
        <VBox spacing="10" padding="10">
            <Label text="Spending by Category"/>
            <PieChart fx:id="categoryChart" prefWidth="300" prefHeight="300"/>
        </VBox>
    </right>

    <!-- Bottom: Form + Buttons -->
    <bottom>
        <VBox spacing="10" padding="10">
            <HBox spacing="10">
                <TextField fx:id="dateField" promptText="Date (YYYY-MM-DD)" />
                <TextField fx:id="categoryField" promptText="Category" />
                <TextField fx:id="amountField" promptText="Amount" />
                <TextField fx:id="descriptionField" promptText="Description" />
            </HBox>
            <HBox spacing="10">
                <Button text="Add Transaction" onAction="#handleAdd"/>
                <Button text="Export CSV" onAction="#handleExportCSV"/>
            </HBox>
        </VBox>
    </bottom>

</BorderPane>
